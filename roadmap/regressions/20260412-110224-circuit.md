# Regression — circuit — 20260412-110224

**Cluster:** regressions · **Status:** shipped · **Severity:** critical

## Detected against

- Repo: `/Users/jason/github/circuit`
- Current krit SHA: `e49ec7d`
- Baseline krit SHA: `a2bba8e2e6d832efbd1c17768195d1fcbccb414d`
- Baseline timestamp: `2026-04-12T06:13:34Z`

## Totals

- Baseline findings: 129
- Current findings:  150 (16.3% delta)
- Baseline scan: 203 ms
- Current scan:  421 ms (107.4% delta)

## Per-rule deltas

| Rule | Before | After | Delta | Kind |
|------|-------:|------:|------:|------|
| ComposeClickableWithoutMinTouchTarget | 0 | 21 | 21 | new-fp-or-expanded |

## Regression flags

- perf_regression: true
- total_regression: true
- rule_regression: true

## Action

A roadmap-loop iteration will pick this file up as top priority and
ask codex to:

1. Identify the krit commit that introduced the change
   (`git log --oneline` since baseline SHA) and read the diff.
2. Decide whether the regression is an intentional behavior change
   (update the baseline) or an accidental one (revert or fix).
3. Land a fix OR a baseline update, verify `go build` + `go test`,
   and let the loop commit it.

If the root cause is a false positive introduced by a recent rule
change, prefer fixing the rule. If the root cause is lost findings
because a rule was weakened, prefer either reverting or adding a
fixture test that pins the old behavior so it can't regress silently
again.

## Resolution Note

- Root cause commit: `f2bb637fc6d44152e21dbbf353ff42895fb3821a`.
- That change broadened `ComposeClickableWithoutMinTouchTarget` to flag every `Modifier.clickable` chain without an explicit size, which produced 21 false positives in `circuit` and inflated scan time.
- The rule was corrected in `1eaa5232d05a7c24a33ad1adfda8fced2f70f722` to only flag explicit `width` / `height` / `size` values below `48.dp`. Fixture coverage now includes `tests/fixtures/positive/compose/ComposeClickableWithoutMinTouchTarget.kt` and `tests/fixtures/negative/compose/ComposeClickableWithoutMinTouchTarget.kt`, including a negative `Modifier.clickable { ... }` case because implicit size is too layout-dependent to treat as a reliable violation.
- Baseline was not updated; the regression was an accidental rule expansion, so the fix stayed in the rule and fixture coverage.
