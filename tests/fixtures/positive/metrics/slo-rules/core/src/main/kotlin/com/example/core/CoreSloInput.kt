package com.example.core

import java.time.*

// TODO: intentionally noisy so the aggregate fixture exceeds the :core budget.
fun BAD_name() = OffsetDateTime.of(2024, 3, 14, 15, 9, 26, 0, ZoneOffset.UTC)
