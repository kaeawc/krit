# Regression — anvil — 20260412-155104

**Cluster:** regressions · **Status:** accepted · **Severity:** critical

## Detected against

- Repo: `/Users/jason/github/anvil`
- Current krit SHA: `e3682d1`
- Baseline krit SHA: `d5707fa7f2793a54e29bb8872e14707a11c26eb9`
- Baseline timestamp: `2026-04-12T05:54:37Z`

## Totals

- Baseline findings: 176
- Current findings:  177 (0.6% delta)
- Baseline scan: 369 ms
- Current scan:  497 ms (34.7% delta)

## Per-rule deltas

No rule-level deltas above threshold. The regression is total-count or perf only.

## Regression flags

- perf_regression: true
- total_regression: false
- rule_regression: false

## Resolution

Accepted as an intentional regression for added functionality.

Root cause: `e49ec7d` (`roadmap loop: di-hygiene/anvil-merge-component-empty-scope`)
adds `AnvilMergeComponentEmptyScope`, which contributes one new finding on
`anvil` and raises scan time enough to cross the current perf threshold.

Current clean measurements against `/Users/jason/github/anvil`:

- Baseline `d5707fa`: 176 findings, 356 ms
- Introducing commit `e49ec7d`: 177 findings, 602 ms
- Current `HEAD`: 177 findings, 512 ms

This delta is acceptable for the additional cross-file Anvil coverage, so the
correct follow-up is to refresh the stored integration baseline for `anvil`
rather than revert the rule.

## Action

A roadmap-loop iteration will pick this file up as top priority and
ask codex to:

1. Identify the krit commit that introduced the change
   (`git log --oneline` since baseline SHA) and read the diff.
2. Decide whether the regression is an intentional behavior change
   (update the baseline) or an accidental one (revert or fix).
3. Land a fix OR a baseline update, verify `go build` + `go test`,
   and let the loop commit it.

If the root cause is a false positive introduced by a recent rule
change, prefer fixing the rule. If the root cause is lost findings
because a rule was weakened, prefer either reverting or adding a
fixture test that pins the old behavior so it can't regress silently
again.
