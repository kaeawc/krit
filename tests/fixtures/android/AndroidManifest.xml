<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app"
    android:versionCode="42"
    android:versionName="1.2.3">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <permission
        android:name="com.example.app.CUSTOM_PERMISSION"
        android:protectionLevel="signature" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:debuggable="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Exported activity with intent-filter (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Non-exported activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Activity with intent-filter but no explicit exported attr (implicit export) -->
        <activity android:name=".DeepLinkActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="myapp" android:host="open" />
            </intent-filter>
        </activity>

        <!-- Exported service with permission -->
        <service
            android:name=".SyncService"
            android:exported="true"
            android:permission="com.example.app.CUSTOM_PERMISSION" />

        <!-- Exported service WITHOUT permission (security issue) -->
        <service
            android:name=".MessagingService"
            android:exported="true" />

        <!-- Exported receiver WITHOUT permission (security issue) -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Non-exported receiver -->
        <receiver
            android:name=".LocalReceiver"
            android:exported="false" />

        <!-- Exported content provider WITHOUT permission (security issue) -->
        <provider
            android:name=".DataProvider"
            android:exported="true"
            android:authorities="com.example.app.provider" />

        <!-- Non-exported content provider with permission -->
        <provider
            android:name=".InternalProvider"
            android:exported="false"
            android:permission="com.example.app.CUSTOM_PERMISSION"
            android:authorities="com.example.app.internal" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
        </intent>
    </queries>
</manifest>
