# Regression — CatchUp — 20260412-024227

**Cluster:** regressions · **Status:** shipped · **Severity:** critical

## Detected against

- Repo: `/Users/jason/github/CatchUp`
- Current krit SHA: `ef101fd`
- Baseline krit SHA: `7f6dfd6e56c73b3148a279558c40168f7bf75b89`
- Baseline timestamp: `2026-04-12T05:45:23Z`

## Totals

- Baseline findings: 193
- Current findings:  189 (-2.1% delta)
- Baseline scan: 130 ms
- Current scan:  148 ms (13.8% delta)

## Per-rule deltas

| Rule | Before | After | Delta | Kind |
|------|-------:|------:|------:|------|
| UnsafeCallOnNullableType | 63 | 59 | -4 | lost-findings |

## Regression flags

- perf_regression: false
- total_regression: false
- rule_regression: true

## Investigation

- Rebuilt and ran both the baseline SHA `7f6dfd6e56c73b3148a279558c40168f7bf75b89`
  and current SHA `ef101fd` against the current local CatchUp checkout with
  `./krit -no-cache -f json /Users/jason/github/CatchUp`.
- Both builds now report the same result: `193` total findings and `63`
  `UnsafeCallOnNullableType` findings.
- Diffing the full `UnsafeCallOnNullableType` finding lists between those two
  builds produced no differences.
- `git log --oneline 7f6dfd6e56c73b3148a279558c40168f7bf75b89..HEAD -- internal/rules/`
  shows no change to `potentialbugs_nullsafety.go` or its tests in this range.

This report is currently non-reproducible, so I did not update the rule or the
baseline JSON. There is no confirmed rule-behavior change to land here.

## Action

A roadmap-loop iteration will pick this file up as top priority and
ask codex to:

1. Identify the krit commit that introduced the change
   (`git log --oneline` since baseline SHA) and read the diff.
2. Decide whether the regression is an intentional behavior change
   (update the baseline) or an accidental one (revert or fix).
3. Land a fix OR a baseline update, verify `go build` + `go test`,
   and let the loop commit it.

If the root cause is a false positive introduced by a recent rule
change, prefer fixing the rule. If the root cause is lost findings
because a rule was weakened, prefer either reverting or adding a
fixture test that pins the old behavior so it can't regress silently
again.
